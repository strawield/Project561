@model IEnumerable<bookReview.Models.Comment>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
@using (Html.BeginForm())
{
    <head>
        <meta name="viewport" content="width=device-width" />
        <title>Comments</title>
    </head>
    @Html.TextBox("comment")
    <div class="form-group">
        <div class="col-md-offset-2 col-md-10">
            <input type="submit" value="Submit" class="btn btn-default" />
        </div>
    </div>
    <body>
        <p>
            @Html.ActionLink("Create New", "CreateBook")
        </p>
        <table class="table">
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.BookTitle)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.CommentText)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.UserN)
                </th>
                <th></th>
            </tr>

            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.BookTitle)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.CommentText)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.UserN)
                    </td>
                    <td>
                        @Html.ActionLink("Back to List", "BookList") |
                        @Html.ActionLink("Delete", "DeleteC", new { id = item.CommentID })
                    </td>
                </tr>
            }

        </table>
        <div>
            @Html.ActionLink("Log Out", "LogOut")
        </div>
    </body>
}
</html>
